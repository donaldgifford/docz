line=9 number=1 question="PR granularity" resolution="One `dont-release` PR per phase, merged in order; Phase 1 lands as per-package commits"
line=10 number=2 question="v1.2.2 binary for the golden capture" resolution="`go install …/cmd/docz@v1.2.2` into a temporary `GOBIN` from `make parity-capture`; provenance in `test/parity/README.md`"
line=11 number=3 question="Parity driver shape" resolution="A Go test in `test/parity/` behind `//go:build parity`, `os/exec` over the binary, file goldens, `-update` gated on `DOCZ_PARITY_BIN`"
line=12 number=4 question="Pre-release build trigger" resolution="A separate `prerelease.yml` on `v*-beta.*` tags running only goreleaser; `release.yml` untouched"
line=13 number=5 question="`docz task list`" resolution="Deferred past this document; removed from Phase 5"
line=14 number=6 question="Corpus migration placement" resolution="In the swap PR as separate commits: the mechanical `validate --fix` diff, then the hand fixes"
line=15 number=7 question="`docz validate` in CI" resolution="Yes, non-strict, appended to `make ci` after `parity` in Phase 5"
line=16 number=8 question="Plan delta in the parity suite" resolution="A named `types.plan` normaliser, the fourth permitted delta, applied from Phase 4"
line=17 number=9 question="Hand-marking this document" resolution="No; it migrates with the corpus in Phase 5"
line=18 number=10 question="Per-type tier dispatch in `docz validate`" resolution="An explicit five-arm switch in `cmd/validate.go` on the new `DocFindings.Schema` with the type name as fallback; every built-in has a package (`impl`, `rfc`, `adr`, `design`, `investigation`), a custom type on `schema: <built-in>` gets that arm, one on its own schema gets the generic tier only"
line=19 number=0 question="**Amendment 2026-09-20: documents without markers**" resolution="A document with no `docz:` marker parses by inference from its headings (`kinds.InferRegions`; `Doc.Inferred`; one `region.inferred` warning, `region.missing` still an error for an absent heading; markers once present are authoritative); `docz validate --fix` writes what inference found and re-validates, replacing `update --regions` (DESIGN-0015 OQ 4 → c). Phase 1, 3, and 5 tasks updated"
line=20 number=0 question="**Amendment 2026-09-20: claude-skills deferred, #97 retargeted**" resolution="The docz skills plugin is not touched and no claude-skills issue is filed in this unit: it serves v1 users and marked templates or five types there would break them before v2 ships; its update is a follow-up after `v2.0.0-beta.1` (Out of Scope; ADR-0003 Decision 5 amendment). docz issue #97 (`update --check`) was retargeted the same day to `docz validate` (`toc.stale`, `IndexDrift`) and is closed by Phase 5"
