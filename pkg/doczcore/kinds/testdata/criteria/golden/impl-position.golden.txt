line=3 executable=true command="make ci" text="`make ci` is green and the `test/consumer` calls that existed at v1.2.2 compile unchanged — nothing in the five frozen packages changed shape."
line=5 executable=true command="go test ./pkg/doczcore/docparse/... ./pkg/doczcore/kinds/... ./pkg/doczcore/validate/... ./pkg/impl/... ./pkg/rfc/... ./pkg/adr/... ./pkg/design/... ./pkg/investigation/..." text="`go test ./pkg/doczcore/docparse/... ./pkg/doczcore/kinds/... ./pkg/doczcore/validate/... ./pkg/impl/... ./pkg/rfc/... ./pkg/adr/... ./pkg/design/... ./pkg/investigation/...` passes, fuzz seed corpora included."
line=7 executable=false command="" text="Every embedded template validates clean against its skeleton, the derivation test binds each template to its skeleton, and each type package parses its own rendered template with every field present."
line=10 executable=false command="" text="Every `.orig.md` fixture parses through inference to the same facts as its migrated sibling with `Inferred` set, and each package's heading table equals `SpecFromTemplate` over its embedded template."
line=13 executable=true command="go test ./pkg/doczcore/toc/..." text="`go test ./pkg/doczcore/toc/...` passes with the golden byte-identical after the `Regions` re-point."
line=15 executable=false command="" text="The `docwrite` status and checktask goldens pass unchanged through the path wrappers."
line=17 executable=true command="go test ./pkg/doczcore/ -run 'TestLayer'" text="`go test ./pkg/doczcore/ -run 'TestLayer'` passes."
line=18 executable=true command="make parity" text="`make parity` is green against `build/bin/docz` with marker lines as the only delta."
line=20 executable=true command="make test-consumer" text="`make test-consumer` exercises `validate`, `kinds`, and all five type packages from outside the module."
