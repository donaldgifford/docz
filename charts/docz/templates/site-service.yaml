apiVersion: v1
kind: Service
metadata:
  name: {{ include "docz.componentFullname" (dict "ctx" . "component" "site") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "docz.labels" (dict "ctx" . "component" "site") | nindent 4 }}
spec:
  type: {{ .Values.site.service.type }}
  ports:
    - port: {{ .Values.site.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docz.selectorLabels" (dict "ctx" . "component" "site") | nindent 4 }}
