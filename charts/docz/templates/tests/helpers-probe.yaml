{{- /* Throwaway: renders the shared helpers for tests/chart/helpers_test.yaml
until real templates exist (IMPL-0021 Phase 1). Deleted in Phase 2. */ -}}
{{- if .Values.helpersProbe }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "docz.componentFullname" (dict "ctx" $ "component" "api") }}
  labels:
    {{- include "docz.labels" (dict "ctx" $ "component" "api") | nindent 4 }}
data:
  fullname: {{ include "docz.fullname" . | quote }}
{{- end }}
